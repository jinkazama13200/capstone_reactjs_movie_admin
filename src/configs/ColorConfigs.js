import { blue, grey, red, green } from "@mui/material/colors";

const colorConfigs = {
  sidebar: {
    bgcolor: grey[800],
  },
  text: {
    bgColor: grey[800],
    primary: "#fff",
  },
  usertype: {
    bgcolor: {
      admin: red["A400"],
      customer: green["A700"],
    },
  },
};

export default colorConfigs;
